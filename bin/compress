#!/usr/local/bin/node

var sys = require('sys'),
	compiler = require('../lib/compiler')

var file = process.argv[2],
	compiled = compiler.compileJSFile(file)

compiler.compressJS(compiled, function(compressedCode) {
	sys.puts(compressedCode)
})
